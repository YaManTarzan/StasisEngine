using System.Threading;

namespace StasisEngine
{
    public static class StasisEngine
    {
        public static void Run()
        {
            //Thread inputThread = new Thread(Input.Update);
            //inputThread.Start();
        }
    }
}